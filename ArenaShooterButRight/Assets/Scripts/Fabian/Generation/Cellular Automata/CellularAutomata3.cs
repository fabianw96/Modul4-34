using UnityEngine;

namespace Fabian.Generation.Cellular_Automata
{
    public class CellularAutomata3 : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
